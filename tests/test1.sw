import mod::{sum_i32, sum_i64};

fn main(a: i32, b: i32) {
    var me: i32 = 32;
    var test = me;

    sum_i32(a, b);
    sum_i64(a, b);

}

fn main_2(a: i32, b: i32) {
    var me: i32 = 32;
    var test = me;

    sum_i32(a, b);
    sum_i64(a, b);

}