#include <iostream>
#include <string>
#include <memory>

class Int {
public:
    const char val[];
    Int(char value[]) {
        for (auto elem : value) {
            std::to_integer(value);
        }
    }
};
